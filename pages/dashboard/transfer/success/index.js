const TransferSuccess = () => {
    return <div className="d-flex">TransferSuccess</div>;
}

export default TransferSuccess;

TransferSuccess.getLayout = function getLayout(page) {
  return page;
};  